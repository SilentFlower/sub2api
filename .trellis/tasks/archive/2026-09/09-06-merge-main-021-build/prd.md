# 同步 main 0.2.1 到 build 并保留定制功能

## 目标

将 main 0.2.1 更新同步到 build，消除文本和语义冲突，使升级后的代码同时具备上游新能力与用户确认保留的定制功能。

## 背景与授权

- 当前 build 为 4e9829519707f0743a7cbcabcf3e73b8dd0beeb0（0.2.0），目标 origin/main 为 ab99d56e9626e6cd731592dae8553c9758a0efa2（0.2.1）。
- 预检发现待合入 77 个提交、265 个上游变更文件，双方共同修改 63 个文件，12 个文件有 17 个文本冲突块；自动合并还会产生 GPT-6 同名函数。
- 用户先指定“对于gpt6的改动你可以按main的来”，随后审阅保留功能清单及 GPT-6 提示词差异，并确认“可以开始任务吧”。本任务延续这次已确认的合并范围。

## 需求

- R1：整合固定目标 main 0.2.1 的全部更新，版本与上游一致。
- R2：GPT-6 的模型列表、别名、上下文、能力、推理档位、计费及提示词选择以 main 为准；删除重复定义和旧 build 专属覆盖，更新旧测试断言。
- R3：保留 build 的 AnySearch、本地搜索、web.run/混合工具搜索、Lite 搜索桥接、预算耗尽后继续回答及账号/渠道配置。
- R4：保留账号 JSON Schema 降级及 DeepSeek 缺失推理历史自动降级，保留 Anthropic → Chat 的缓存前缀、工具顺序、会话粘性、SSE 消费和 JSON 折叠。
- R5：保留通用 Responses Lite 模型阻止列表及 HTTP/WebSocket 策略；保留生图主模型、思考预算配置和客户端图片工具识别，以及现有 Lite 不自动注入 hosted 图片工具的边界。
- R6：保留 Grok 强制 Chat、4.5 effort 适配、额度展示、批量登录脚本，反重力 GIF 多帧与重试支持，自定义 UA 放行、出站身份一致性、GPT-5.6 专用提示词。
- R7：保留构建流程、Trellis/Flower、已有任务及容灾资产；接续 main 已有 alpha/search 实现，将 GLM-5.3 新规则整合到实际领域模块；账号弹窗同时支持保留设置与上游新设置。
- R8：冲突处理后中英文最终文案和 key 保持完整，已移除的旧 Codex 邀请重置不恢复。

## 验收标准

- A1（R1）：合并索引无未解决项和冲突标记，VERSION 为 0.2.1；目标 main 的改动全部纳入并有合并基线记录。
- A2（R2）：GPT-6 模型无重复条目/函数；main 上下文、别名、能力、计费和提示词逻辑生效，GPT-5.6 专用提示词继续有效。
- A3（R3–R6）：保留清单逐项核对实际入口和领域实现，相应自动化回归通过。
- A4（R4、R7）：GLM-5.3 low 和 Anthropic thinking 生效；流式及非流式协议回退均记录 main 新增响应头，原 build 转换保持可用。
- A5（R7、R8）：新建和编辑账号可保存、清理并重新读取兼容设置与图片 URL 转 base64 开关；中英文 locale 检查通过。
- A6：Go 单元测试、后端构建/静态检查、前端类型检查/构建/相关测试完成；确有环境或既存问题时记录证据及实际影响，不伪报通过。

## 不在范围

本轮不部署、不执行真实数据库迁移或外部系统操作；提交和推送待最终具体合并结果审阅，不改变目标上游提交，不恢复已删除功能，也不做无关重构。
